name: CD / Staging

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: Build and Push
    permissions:
      contents: read
      packages: write
    secrets: inherit
    uses: ./.github/workflows/cd-build-and-push.yaml
    with:
      target: staging
