### Tiedettävät bash komennot:
 - ```k3d cluster create -a 2```
 - ```kubectl apply -f manifests/deployment.yaml```
 - ```oc apply -f manifests/deployment.yaml```
 - ```kubectl delete -f manifests/deployment.yaml```
 
 ### Infokomentoja
 - ```kubectl get deployments```